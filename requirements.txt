services:
  - type: web
    name: cleancopy-ocr-eflg
    env: docker
    plan: starter
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
      - key: REDIS_URL
        fromService:
          type: redis
          name: cleancopy-redis
          property: connectionString
    healthCheckPath: /healthz
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1

  - type: worker
    name: cleancopy-worker
    env: docker
    plan: starter
    autoDeploy: true
    envVars:
      - key: REDIS_URL
        fromService:
          type: redis
          name: cleancopy-redis
          property: connectionString
    disk:
      name: data
      mountPath: /app/data
      sizeGB: 1
    startCommand: python worker.py

databases:
  - name: cleancopy-redis
    plan: free
    ipAllowList: []
